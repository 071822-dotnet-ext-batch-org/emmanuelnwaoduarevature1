public static void miniMaxSum(List<int> arr)
    {
    
     Array.Sort(arr);
     int min = List<int> arr.Take(4).Sum(item);
     int max = List<int> arr.Skip(1).Sum(item);
     Console.WriteLine($"{min} , {max}");
}

